# env files — never commit
.env*

# misc
.DS_Store
*.pem

# Local Netlify folder
.netlify

# Internal workspace documents — held privately by the Empowr CIC hub.
# This repo is PUBLIC; these carry operational detail and must never be
# tracked here. See ../../CONTEXT.md and ../../workspace-docs/empowr-efn/.
DEVLOG.md
memory.md
